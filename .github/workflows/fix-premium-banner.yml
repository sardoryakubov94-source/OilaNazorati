name: Fix Premium Banner

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - name: Kodni olish
        uses: actions/checkout@v4

      - name: Original dashboardni tiklash va Premium bannerini tuzatish
        run: |
          echo "Faqat qo'lda ishga tushiriladi; avtomatik push paytida kodni o'zgartirmaydi."
